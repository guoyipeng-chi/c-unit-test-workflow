# C Unit Test Workflow - Python Dependencies
# 使用方法: pip install -r requirements.txt

# 不需要额外的Python依赖，所有工作都是基于标准库
# 但推荐安装以下工具用于增强功能：

# 代码质量分析
pylint>=2.12.0      # 代码质量检查
black>=22.0         # 代码格式化

# 测试相关 (可选)
pytest>=7.0         # 如果想测试工作流本身
clang
requests

# 说明：
# 当前工作流脚本仅使用Python标准库，无强制依赖
# 但为了使用CMake和运行C++测试，需要安装系统工具
